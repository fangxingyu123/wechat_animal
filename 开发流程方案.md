# 《喵汪搭伙跳》开发流程方案

**技术栈**：Cocos Creator 3.8 + 微信开发者工具 + 微信小游戏云开发
**开发策略**：先编码后贴图（全程使用占位色块/简笔画替代美术资源，功能验证通过后替换正式资源）

---

## 阶段〇：环境搭建与项目初始化（第1天）

### 步骤 0.1 安装开发工具

1. 安装 **Cocos Creator 3.8.x**（官网下载 Dashboard → 安装 3.8 最新稳定版）
2. 安装 **微信开发者工具**（稳定版），登录微信账号
3. 在微信公众平台注册 **小游戏 AppID**（如已有则跳过）

### 步骤 0.2 创建 Cocos 项目

1. Cocos Dashboard → 新建项目 → 选择 **3D Empty**（虽是2D游戏，3.8用3D空项目+2D组件更灵活）
2. 项目名称：`PawJump`，路径选择工作区目录
3. 打开项目后，菜单 **项目 → 项目设置**：
   - **功能裁剪**：关闭 3D Physics、3D Particle，保留 2D Physics（Box2D）
   - **宏配置**：CLEANUP_IMAGE_CACHE = true（内存优化）

### 步骤 0.3 配置微信小游戏构建

1. 菜单 **项目 → 构建发布 → 添加构建任务**
2. 平台选择 **微信小游戏**
3. 填入 AppID
4. 勾选 **分离引擎**（减小首包体积）
5. 设置 **启动场景** 为 `loading`（后面创建）
6. 构建一次，确认微信开发者工具能正常预览空白项目

### 步骤 0.4 项目目录结构规划

在 `assets/` 下创建以下文件夹结构：

```
assets/
├── scenes/             # 场景文件
│   ├── loading.scene       # 加载场景
│   ├── home.scene          # 主界面
│   ├── game.scene          # 核心游戏场景（单人/多人复用）
│   └── result.scene        # 结算界面
├── scripts/            # 脚本
│   ├── core/               # 核心玩法
│   │   ├── GameManager.ts      # 游戏主控
│   │   ├── InputManager.ts     # 输入管理（蓄力/跳跃）
│   │   ├── CameraController.ts # 镜头跟随
│   │   └── ScoreManager.ts     # 积分/距离/combo
│   ├── block/              # 方块系统
│   │   ├── BlockFactory.ts     # 方块工厂（生成/回收）
│   │   ├── BlockBase.ts        # 方块基类
│   │   ├── BlockNormal.ts      # 基础方块
│   │   ├── BlockSpecial.ts     # 特殊方块（加速/积分/爱心等）
│   │   ├── BlockDanger.ts      # 危险方块（刺球/滑溜/消失）
│   │   ├── BlockMario.ts       # 联动方块（蘑菇/砖块/管道）
│   │   └── BlockMulti.ts       # 多人专属方块
│   ├── character/          # 角色系统
│   │   ├── CharacterBase.ts    # 角色基类
│   │   ├── CharacterData.ts    # 角色数据配置
│   │   ├── SkillSystem.ts      # 技能系统
│   │   └── CharacterSkin.ts    # 皮肤管理
│   ├── challenge/          # 挑战系统
│   │   ├── ChallengeManager.ts # 挑战管理器
│   │   ├── EnemyBase.ts        # 敌人基类
│   │   ├── EnemyHamster.ts     # 刺球仓鼠
│   │   ├── EnemyParrot.ts      # 飞毛球鹦鹉
│   │   ├── EnemySucculent.ts   # 伸缩多肉
│   │   └── TrapSystem.ts       # 陷阱系统
│   ├── level/              # 关卡系统
│   │   ├── LevelManager.ts     # 关卡管理
│   │   ├── LevelConfig.ts      # 关卡配置数据
│   │   ├── WorldConfig.ts      # 世界配置（5大主题）
│   │   ├── BossBase.ts         # BOSS基类
│   │   └── DifficultyAdapter.ts # 难度自适应
│   ├── multi/              # 多人模式
│   │   ├── MultiManager.ts     # 多人管理器
│   │   ├── SyncManager.ts      # 同步管理
│   │   ├── RescueSystem.ts     # 救援系统
│   │   └── SkillCombo.ts       # 技能联动
│   ├── system/             # 系统模块
│   │   ├── CollectSystem.ts    # 收集系统（零食/钻石/道具）
│   │   ├── AchieveSystem.ts    # 成就系统
│   │   ├── RankSystem.ts       # 排行榜
│   │   ├── ShopSystem.ts       # 商店/兑换
│   │   └── ReviveSystem.ts     # 复活系统
│   ├── social/             # 社交模块
│   │   ├── ShareManager.ts     # 分享裂变
│   │   └── InviteManager.ts    # 邀请系统
│   ├── ui/                 # UI脚本
│   │   ├── UIHome.ts           # 主界面
│   │   ├── UIGame.ts           # 游戏HUD
│   │   ├── UIResult.ts         # 结算界面
│   │   ├── UIShop.ts           # 商店
│   │   └── UIGuide.ts          # 新手引导
│   ├── data/               # 数据管理
│   │   ├── DataManager.ts      # 本地数据读写
│   │   ├── CloudManager.ts     # 云开发接口
│   │   └── ConfigTables.ts     # 配置表管理
│   └── utils/              # 工具类
│       ├── ObjectPool.ts       # 对象池
│       ├── EventCenter.ts      # 事件中心
│       └── MathUtils.ts        # 数学工具
├── prefabs/            # 预制体
│   ├── blocks/             # 各种方块预制体
│   ├── characters/         # 角色预制体
│   ├── enemies/            # 敌人预制体
│   ├── effects/            # 特效预制体
│   └── ui/                 # UI预制体
├── textures/           # 图片资源（先放占位图）
│   ├── placeholder/        # 占位色块
│   ├── characters/         # 角色贴图（后期替换）
│   ├── blocks/             # 方块贴图
│   ├── ui/                 # UI图标
│   └── backgrounds/        # 背景图
├── audio/              # 音频资源
│   ├── bgm/                # 背景音乐
│   └── sfx/                # 音效
├── animations/         # 动画资源
└── data/               # 配置表（JSON）
    ├── levels.json         # 关卡配置
    ├── characters.json     # 角色配置
    ├── blocks.json         # 方块配置
    ├── achievements.json   # 成就配置
    └── shop.json           # 商店配置
```

### 步骤 0.5 占位资源制作

用 Cocos 内置工具创建最简占位资源（后期全部替换正式美术）：
1. 方块：不同颜色的矩形色块（80×20px），颜色区分类型（白=基础，粉=爱心，黄=加速，灰=刺球，蓝=滑溜，红=蘑菇，绿=管道……）
2. 角色：40×40px 的圆形色块（橘色=胖橘，棕色=阿柴）
3. 背景：纯色渐变矩形
4. UI：文字按钮即可

### 步骤 0.6 初始化 Git 仓库

```bash
cd PawJump
git init
# 创建 .gitignore（忽略 build/、temp/、library/、local/、node_modules/）

https://github.com/fangxingyu123/wechat_animal.git

git add . && git commit -m "init: Cocos Creator 3.8 project setup"
```

---

## 阶段一：核心跳跃手感（第2-4天）

> 这是整个游戏的根基，必须反复打磨到手感舒适才能继续。

### 步骤 1.1 创建游戏场景骨架

1. 打开 `game.scene`，创建节点层级：

```
Canvas (UITransform: 1280×720)
├── GameLayer               # 游戏世界（跟随相机移动）
│   ├── BlockContainer      # 方块容器
│   ├── CharacterNode       # 当前角色
│   └── EnemyContainer      # 敌人容器
├── UILayer                 # HUD（固定屏幕）
│   ├── ScoreLabel          # 积分显示
│   ├── DistanceLabel       # 距离显示
│   ├── ComboLabel          # combo显示
│   ├── SkillBtn            # 技能按钮
│   ├── SwitchBtn           # 角色切换按钮
│   └── PowerBar            # 蓄力条
└── Camera (2D)             # 主相机
```

2. 设置 Camera 为 2D 模式，投影选 Ortho

### 步骤 1.2 实现蓄力-跳跃核心机制 `InputManager.ts`

精确实现设计文档中的操作逻辑：

```typescript
// 核心状态机：IDLE → CHARGING → JUMPING → LANDING → IDLE
// 关键参数（对应详细设计B.1）：
//   按压时长 → 跳跃距离映射：
//     <0.3s → 0.5格, 0.5s → 1格, 1s → 2格, 1.5s → 3格, 2s(上限) → 4格
//   误触保护：屏幕左右10%不触发；松手后0.1s内不可再次蓄力
//   长按反馈：>1.5s蓄力条变橙色，>2s变红色+震动
```

1. 监听 `touch_start` → 记录起始时间，开始蓄力条动画
2. 实时计算按压时长 → 更新蓄力条 UI（绿→橙→红渐变）
3. 超过 2 秒时触发微信震动 API `wx.vibrateShort()`
4. 监听 `touch_end` → 计算最终按压时长 → 映射跳跃距离
5. 屏幕左右 10% 区域检测：`touchPos.x < screenWidth * 0.1 || touchPos.x > screenWidth * 0.9` 则忽略
6. 松手后设 0.1 秒冷却标记，防止连续误触

### 步骤 1.3 实现 X 轴跳跃物理 `CharacterBase.ts`

1. 角色跳跃采用**贝塞尔曲线**模拟抛物线（非物理引擎刚体，手感更可控）：
   - 起点：当前方块中心顶部
   - 终点：目标位置（按蓄力距离计算）
   - 控制点：起终点中间偏上（高度 = 跳跃距离 × 0.6，可调）
   - 持续时间：0.3-0.5 秒（距离越远略长）
2. 使用 `tween` 动画驱动角色沿贝塞尔曲线移动
3. 跳跃过程中角色有轻微旋转动画（萌系翻滚感）

### 步骤 1.4 实现落地判定

1. 跳跃动画结束时，检测角色中心点是否在某个方块的有效范围内
2. **掉落判定**（设计文档）：角色重心超出方块边缘≥50% → 判定掉落
3. 落地成功 → 播放"噗"落地效果 → 相机跟随 → 等待下次蓄力
4. 落地失败 → 触发掉落动画（慢速下落 + 惊讶表情）→ 进入失败/复活流程

### 步骤 1.5 实现相机跟随 `CameraController.ts`

1. 相机 X 轴平滑跟随角色（`lerp` 插值，系数 0.1）
2. Y 轴固定或小幅跟随（方块高度变化不大）
3. 角色跳跃时相机略微前移（预判），落地后回到角色中心

### 步骤 1.6 手感调试

1. 在场景中手动摆放 10 个等距基础方块（占位色块）
2. 反复测试蓄力→跳跃→落地流程
3. 调参重点：蓄力条灵敏度、跳跃弧线高度、落地判定宽容度、相机跟进速度
4. **验收标准**：普通玩家试玩10次，8次以上能准确跳到目标方块

---

## 阶段二：方块系统（第5-7天）

### 步骤 2.1 方块基类与对象池

1. `BlockBase.ts`：包含位置、类型枚举、外观节点、碰撞区域、状态（正常/消失/移动中）
2. `ObjectPool.ts`：方块对象池，避免频繁创建销毁，预分配 30 个方块节点
3. 方块预制体：一个通用预制体 + 脚本控制外观切换（颜色/图标由类型决定）

### 步骤 2.2 方块工厂 `BlockFactory.ts`

核心职责：根据当前世界/难度/概率配置，生成下一个方块

1. 读取 `blocks.json` 配置表（各方块在不同难度下的生成概率）
2. **生成算法**：
   - 根据当前世界难度系数 D，加载对应概率表
   - 随机投掷 → 确定方块类型
   - **保底机制**：连续 3 个非基础方块后，强制生成 1 个基础方块
   - 计算与前一个方块的间距（1-4 格随机，难度越高最大间距越大）
   - 方块高度偏移（±10px 随机，增加视觉层次）
3. 生成的方块从对象池取出 → 设置位置/类型/外观 → 放入 BlockContainer
4. 角色通过后，回收屏幕左侧已不可见的方块

### 步骤 2.3 实现各类型方块效果

**基础方块** `BlockNormal.ts`：
- 无额外效果，踩击后无变化

**特殊方块** `BlockSpecial.ts`（逐个实现）：
1. **加速方块**：踩击后发事件 `EVENT_BUFF_SPEED` → InputManager 收到后，5 秒内蓄力时间减半
2. **积分方块**：踩击后 ScoreManager 加 50 分 + 播放金币动画
3. **爱心软垫**：踩击后多人模式互助次数+1（单人模式无效果）
4. **道具方块**：踩击后随机掉落一个道具弹窗 → 加入背包
5. **互助方块**：仅多人模式生成，踩击后两人无敌 2 秒

**危险方块** `BlockDanger.ts`：
1. **刺球方块**：踩击检测 → 若角色非阿柴技能态，直接触发掉落
2. **滑溜方块**：踩击后角色自动滑动 0.5 格（随机左右），1 秒后恢复
3. **消失方块**：踩击后启动 1 秒倒计时 + 闪烁动画 → 倒计时结束方块消失

**联动方块** `BlockMario.ts`（仅单人模式）：
1. **蘑菇方块**：踩击后触发道具互动挑战（由 ChallengeManager 接管）
2. **砖块方块**：踩击后触发隐藏陷阱挑战
3. **管道方块**：踩击后触发敌人突袭挑战

### 步骤 2.4 验证方块系统

1. 在场景中让 BlockFactory 自动连续生成 50 个方块
2. 跳跃穿越所有方块，验证每种方块效果是否正确触发
3. 检查对象池回收是否正常（内存不增长）

---

## 阶段三：角色与技能系统（第8-10天）

### 步骤 3.1 角色数据配置 `CharacterData.ts`

建立 `characters.json`，写入 6 个角色的全部参数：

```json
[
  {
    "id": "pangju", "name": "胖橘", "type": "cat",
    "speed": 10, "jumpHeight": 30, "tolerance": "medium",
    "skill": {
      "name": "轻盈跳", "type": "light_jump",
      "levels": [
        { "invincible": 1.0, "cd": 8 },
        { "invincible": 1.5, "cd": 7 },
        { "invincible": 2.0, "cd": 6 }
      ]
    },
    "unlock": "free"
  }
  // ... 其余5个角色
]
```

### 步骤 3.2 角色基类实现 `CharacterBase.ts`

1. 角色状态机：IDLE / CHARGING / JUMPING / LANDING / FALLING / SKILL / DEAD
2. 根据 `CharacterData` 初始化：速度、跳跃高度、容错率
3. 角色外观节点（Sprite）：先用占位色块，标注角色名
4. 待机动画占位：每 3 秒触发一次简单缩放动画（模拟晃尾巴）

### 步骤 3.3 技能系统 `SkillSystem.ts`

逐个实现 6 个角色技能：

| 角色 | 技能实现要点 |
|------|-------------|
| **胖橘·轻盈跳** | 释放后将下次跳跃距离强制设为最大值（4格）+ 设角色无敌标记（持续1/1.5/2秒），无敌期间碰敌人不触发掉落 |
| **阿柴·稳落术** | 释放后跳跃落地阶段，自动寻找最近方块中心（吸附范围50/60/70px）修正落点 + 踩危险方块不触发效果而是销毁方块 |
| **布偶猫·陷阱预警** | 释放后遍历屏幕内所有隐藏陷阱方块，将其 opacity 设为 50%（显示轮廓），持续3秒 + 掉落时延迟1秒再判定 |
| **柯基·冲刺跳** | 释放后3秒内：InputManager的蓄力时间映射系数×0.3（缩短70%）+ 跳跃速度×1.5 |
| **英短·群体护盾** | 释放后给自身+队友添加"护盾"标记（挡1次伤害），持续2秒，受伤时检查护盾消除而非掉落 |
| **萨摩耶·伤害反弹** | 释放后2秒内，碰触敌人→敌人销毁+积分+100，而非角色受伤 |

技能CD管理：
- 释放后启动CD倒计时 → UI按钮灰色+倒计时数字
- 切换角色时，技能CD不重置（继续倒计时）

### 步骤 3.4 角色切换逻辑

1. 点击左下角切换按钮 → 检查 CD（3秒）→ 播放切换动画（0.5秒渐变）→ 替换角色数据/外观/技能
2. CD 期间按钮灰色 + 显示倒计时数字
3. 切换后保持当前位置不变，技能CD继续

### 步骤 3.5 验证角色系统

1. 场景中放置各种方块，测试胖橘+阿柴的技能效果
2. 测试角色切换流程（CD、动画、技能保持）
3. 检查刺球方块对不同角色的差异表现

---

## 阶段四：关卡系统与单人闯关（第11-16天）

### 步骤 4.1 关卡配置表 `levels.json`

为 89 个关卡建立配置数据：

```json
{
  "worlds": [
    {
      "id": "yard", "name": "庭院世界", "difficulty": 1,
      "bgColor": "#90EE90", "bgmKey": "bgm_yard",
      "levels": [
        {
          "id": 1, "targetDistance": 50, "targetSnacks": 5,
          "challengeCount": 0, "isGuide": true, "guideStep": "basic_jump",
          "blockProbability": { "normal": 0.9, "special": 0.1, "danger": 0, "mario": 0 }
        }
        // ... 20个常规关 + 1个BOSS关
      ]
    }
    // ... 公园、宠物店、森林、星空
  ]
}
```

### 步骤 4.2 关卡管理器 `LevelManager.ts`

1. 加载 `levels.json` → 根据当前世界/关卡ID读取配置
2. 初始化关卡：设定目标距离、目标零食数、可触发挑战类型
3. 实时检测通关条件：距离≥目标 && 零食≥目标 && 挑战已完成
4. 通关 → 结算界面 → 解锁下一关
5. 失败 → 判断复活条件 → 复活或返回

### 步骤 4.3 世界选择与关卡选择 UI

1. 主界面"闯关"入口 → 世界选择界面（5个世界按钮，未解锁的灰色+锁图标）
2. 点击世界 → 关卡列表（21个按钮，星级显示，未解锁灰色）
3. 点击关卡 → 加载游戏场景 → 读取关卡配置 → 开始闯关

### 步骤 4.4 难度自适应 `DifficultyAdapter.ts`

1. 记录每关的挑战次数（本地存储）
2. 连续失败2次 → 标记"降难模式"：方块工厂概率表中危险方块概率-5%~10%，技能CD缩短1秒
3. 通关后清除降难标记，恢复标准难度
4. 某世界全关无复活无失误通关 → 解锁"精英难度"标记

### 步骤 4.5 新手引导 `UIGuide.ts`

1. 检测是否首次进入游戏（本地存储标记）
2. 依次弹出3步引导遮罩：
   - 第1步：高亮屏幕中央 → "长按蓄力"文字+动画手指
   - 第2步：高亮松手区域 → "松手跳跃"
   - 第3步：高亮左下角切换按钮 → "点击切换角色"
3. 引导完成 → 发放奖励 → 标记引导已完成 → 激活"新手buff"（5分钟蓄力延长0.5秒）

### 步骤 4.6 随机挑战生成 `ChallengeManager.ts`

实现设计文档中的算法：

```
输入：关卡难度系数D, 通关率P, 失误率E
挑战难度 = D × (1 - P × 0.3) × (1 + E × 0.2)
D<3 → 不出高难度挑战
D≥7 → 可出组合挑战
保底：连续3方块必有1基础方块；同屏≤2种挑战
难度曲线：前30%低难度 → 中间50%标准 → 后20%高难度
```

1. 关卡开始时根据配置计算挑战触发点
2. 角色到达触发点 → 生成对应挑战（敌人/陷阱/限时等）
3. 破解成功 → 积分+50 → 继续闯关
4. 破解失败 → 判定为失误

### 步骤 4.7 敌人AI实现

1. **刺球仓鼠** `EnemyHamster.ts`：水平直线移动（3px/帧），碰到边界折返，移动范围3个方块宽度
2. **飞毛球鹦鹉** `EnemyParrot.ts`：随机飞行轨迹（正弦曲线+随机偏移），每2秒变向，飞行范围5个方块
3. **伸缩多肉** `EnemySucculent.ts`：2秒周期上下伸缩（高度+20px），定时器控制
4. 角色与敌人碰撞检测 → 根据角色状态（是否无敌/反弹/技能态）决定结果

### 步骤 4.8 BOSS战实现 `BossBase.ts`

4个BOSS逐个实现（先做庭院·大金毛验证框架）：

1. BOSS以大型方块形式存在，有血量条（5次踩击弱点击杀）
2. BOSS技能循环：每隔X秒释放一种技能（甩动方块/绒球攻击等）
3. 弱点方块在技能间隙出现（1-3秒），被角色踩击→扣血
4. BOSS血量归零 → 通关动画 → 奖励结算

### 步骤 4.9 积分/结算/复活流程

1. `ScoreManager.ts`：实时累计积分（跳跃+零食+挑战+combo）
2. 失败触发 → `ReviveSystem.ts` 判断可用复活方式 → 弹出对应按钮
3. 通关触发 → 结算界面显示：距离、积分、零食、星评（1-3星）

---

## 阶段五：收集/成就/排行系统（第17-20天）

### 步骤 5.1 本地数据管理 `DataManager.ts`

使用 `wx.setStorageSync` / `wx.getStorageSync` 管理：

```typescript
interface PlayerData {
  snacks: number;           // 零食（每日上限800）
  diamonds: number;         // 钻石
  reviveCoins: number;      // 复活币
  stamina: number;          // 体力（上限5，10min恢复1）
  unlockedChars: string[];  // 已解锁角色ID列表
  charLevels: Record<string, number>; // 角色技能等级
  unlockedSkins: string[];  // 已解锁皮肤
  levelProgress: Record<string, number>; // 关卡进度（0未解锁/1已解锁/2已通关/3精英通关）
  inventory: Record<string, number>;     // 背包道具及数量
  achievements: string[];   // 已解锁成就ID列表
  dailyData: { date: string; snacksEarned: number; adRevives: number; shareCount: number };
  stats: { totalDistance: number; totalSnacks: number; bossKills: number; challengeClears: number };
}
```

### 步骤 5.2 收集系统 `CollectSystem.ts`

1. 零食获取：闯关掉落/通关奖励/成就奖励 → 累加到 `playerData.snacks`，**检查每日800上限**
2. 道具背包：各道具有上限（缓冲棉垫5个、破障骨头3个等），超出不可获取
3. 闯关中使用道具 → 扣除背包数量 → 触发对应效果

### 步骤 5.3 兑换商店 `ShopSystem.ts`

1. 零食兑换页：列出所有可兑换道具+价格+每日上限，点击兑换 → 扣零食加道具
2. 钻石兑换页：列出高级道具/角色/皮肤，无每日上限
3. 碎片兑换页：角色碎片/皮肤碎片数量够了 → 一键解锁

### 步骤 5.4 成就系统 `AchieveSystem.ts`

1. 加载 `achievements.json`（22个成就的条件与奖励）
2. 每次闯关结算时，遍历所有未解锁成就，检查条件是否满足
3. 满足 → 弹出成就解锁动画 → 发放奖励 → 标记已解锁

### 步骤 5.5 排行榜 `RankSystem.ts`

1. 使用微信开放数据域（`wx.getOpenDataContext()`）实现好友排行
2. 开放数据域中读取好友积分数据
3. 主域向开放数据域发消息更新自己的积分
4. 排行榜UI：展示前50名，支持每日/每周/总榜切换

---

## 阶段六：多人模式（第21-28天）

### 步骤 6.1 云开发初始化 `CloudManager.ts`

1. 在微信开发者工具中开通**云开发**
2. 创建云函数：`matchPlayer`（匹配）、`createRoom`（建房）、`syncGame`（同步）
3. 创建数据库集合：`rooms`（房间）、`players`（玩家数据）、`ranks`（排行）

### 步骤 6.2 组队匹配 `MultiManager.ts`

1. **创建队伍**：调用云函数创建房间 → 返回房间ID → 生成分享卡片
2. **邀请好友**：通过 `wx.shareAppMessage` 分享带房间ID的链接
3. **随机匹配**：调用云函数，查找等待中的房间 → 匹配成功加入 → 30秒超时提示失败
4. 匹配成功 → 10秒准备倒计时（可切换角色、发快捷消息）→ 开始闯关

### 步骤 6.3 实时同步 `SyncManager.ts`

使用微信**实时对战引擎**（`wx.createGameServerManager()`）：

1. 帧同步方案：每帧（约60fps）收集本地操作指令 → 发送到服务器 → 服务器广播给双方
2. 同步内容：角色位置（精度±5px）、跳跃动作、技能释放、道具使用
3. **延迟补偿**：本地先执行操作，收到服务器确认后修正
4. **延迟处理**：
   - ≤200ms：正常
   - 200-400ms：显示网络警告图标 + 本地预测对方位置
   - >400ms：暂停同步弹提示；>10秒→提示重连或切单人

### 步骤 6.4 救援系统 `RescueSystem.ts`

1. 一方掉落 → 向对方发送"请求救援"消息 → 对方屏幕显示10秒倒计时
2. 救援方跳跃到掉落点±2方块范围内 → 长按1.5秒触发救援
3. 救援成功 → 失败方复活到救援方位置 → 奖励零食×30
4. 超时 → 失败方需用复活币/广告复活

### 步骤 6.5 技能联动 `SkillCombo.ts`

1. 监听双方技能释放事件，记录释放时间戳
2. 两人技能释放时间差 ≤ 1秒 → 判定联动成功
3. 根据角色组合查表 → 触发对应联动效果
4. 联动生效 → 播放联动特效 → 消耗双方各1次技能 → 各自重新计算CD

### 步骤 6.6 多人专属方块与关卡

1. 方块工厂增加"多人模式"分支：不生成联动方块，额外生成猫/狗专属方块
2. 多人关卡配置：89个双人关卡数据（对应单人世界，但目标值更高）
3. 进度同步：两人共同完成目标才算通关

### 步骤 6.7 快捷消息

1. 5种预设消息按钮（准备就绪/注意陷阱/释放技能/请求救援/一起冲刺）
2. 点击 → 通过同步通道发送 → 对方屏幕弹出消息气泡 + 角色语音提示

---

## 阶段七：社交/商业化/合规（第29-35天）

### 步骤 7.1 分享裂变 `ShareManager.ts`

1. 闯关结算"分享成绩单"按钮 → `wx.shareAppMessage()` 带自定义图片+成绩数据
2. 分享回调：加零食×10（每日上限3次）
3. 邀请注册追踪：分享链接带 `inviterOpenId` → 新用户注册时云函数记录 → 达到1/3/5人阈值发放对应奖励

### 步骤 7.2 支付系统

1. 使用微信小游戏**虚拟支付** `wx.requestMidasPayment()`
2. 商品注册：在微信公众平台注册皮肤/礼包/月卡等商品
3. 支付流程：选择商品 → 拉起支付 → 支付成功回调 → 发放道具 → 云函数记录订单

### 步骤 7.3 广告系统

1. **激励视频广告**：`wx.createRewardedVideoAd()`，用于复活/双倍积分/额外零食
2. **插屏广告**：`wx.createInterstitialAd()`，闯关结束后展示（用户可关闭）
3. 广告加载失败时隐藏对应按钮，不影响游戏流程

### 步骤 7.4 未成年人防沉迷

1. 登录时调用 `wx.getUserInfo()` + 微信实名认证接口判断年龄
2. 游戏内启动计时器，每分钟检查累计时长
3. 未成年用户：1小时弹提醒 → 1.5小时强制下线 → 22:00-8:00禁止登录
4. 付费限制：根据年龄段设置单次/月度充值上限（8-16岁单次≤50月≤200元）

### 步骤 7.5 月卡系统

1. 购买月卡 → 记录到期时间（云数据库）
2. 每日登录检查月卡状态 → 有效则自动发放每日奖励（零食×20、互助+2、道具×2）
3. 月卡用户标记免广告复活

---

## 阶段八：音效与动效占位（第36-38天）

### 步骤 8.1 音效占位

1. 用免费音效素材站（如 freesound.org）下载临时占位音效：
   - 跳跃"嗖"声、落地"噗"声、收集"咔嚓"声、按钮"咔哒"声
   - 5段BGM占位（每世界1段，风格不同）
2. 音频格式：BGM → OGG 128kbps，音效 → MP3 64kbps
3. `AudioManager.ts`：统一管理BGM播放/暂停/切换 + 音效播放

### 步骤 8.2 动效占位

1. 角色待机动画：简单 `tween` 缩放循环（每3秒）
2. 跳跃动画：贝塞尔曲线移动 + 轻微旋转
3. 落地动画：Y轴快速压缩回弹（squash & stretch）
4. 掉落动画：慢速Y轴下移 + 角色旋转 + 淡出
5. combo动画：数字放大弹出 + 震屏效果
6. 技能动画：角色闪白/发光占位

---

## 阶段九：性能优化与适配（第39-42天）

### 步骤 9.1 资源优化

1. 图片全部 TinyPNG 压缩（≥60%），单张≤100KB
2. 代码分包设置：首包≤4MB（只含 loading 场景+核心框架），其余功能懒加载
3. 检查动态合图（Dynamic Atlas）是否开启

### 步骤 9.2 运行优化

1. 对象池回收检查：方块/敌人/特效全部走对象池
2. 同屏对象限制：敌人≤4、陷阱≤3，超出时队列等待
3. 多挑战同时触发时错帧处理：每帧最多初始化1个挑战对象
4. 帧率监控：`cc.game.frameRate` 保持≥30fps

### 步骤 9.3 设备适配

1. 屏幕适配：Canvas 使用 `SHOW_ALL` + 安全区域适配
2. 低配机型检测：内存≤3GB时自动关闭粒子特效，降低图片质量
3. 在微信开发者工具中测试 iOS/Android 真机预览
4. 使用微信性能面板检查内存/帧率/加载时间

### 步骤 9.4 首屏加载优化

1. Loading 场景极简化（只有进度条+Logo占位）
2. 场景预加载：`director.preloadScene("game")`
3. 资源异步加载：`resources.load()` 逐步加载非核心资源
4. 目标：首次加载≤3秒，二次加载≤1秒

---

## 阶段十：测试与修复（第43-56天）

### 步骤 10.1 功能测试清单

逐项验证：

| 模块 | 测试项 |
|------|--------|
| 蓄力跳跃 | 5种按压时长对应距离是否精确；误触保护是否生效；长按反馈是否触发 |
| 方块系统 | 12种方块效果是否正确；生成概率是否符合配置；保底机制是否生效；对象池是否正常回收 |
| 角色技能 | 6个角色6种技能效果是否正确；CD计时是否准确；切换后CD是否保持 |
| 关卡闯关 | 89个关卡是否可逐步解锁；目标检测是否正确；BOSS战是否可通关 |
| 难度自适应 | 连续失败2次是否降难；通关后是否恢复；精英难度是否解锁 |
| 复活系统 | 3种复活方式是否正常触发；次数限制是否生效 |
| 收集系统 | 零食每日800上限；道具背包上限；兑换扣费正确 |
| 成就系统 | 22个成就条件判定是否正确；奖励是否发放 |
| 多人模式 | 组队/匹配/同步/救援/联动全流程 |
| 社交分享 | 分享/邀请/奖励发放链路 |
| 支付 | 购买/发货/月卡每日奖励 |
| 防沉迷 | 时长限制/宵禁/付费限制 |

### 步骤 10.2 灰度测试

1. 构建微信小游戏体验版 → 邀请 3000 名种子用户（≥5天）
2. 重点收集：难度平衡反馈、双人同步流畅度、崩溃日志、关卡卡点
3. 根据反馈调整：难度系数、方块概率、技能数值

### 步骤 10.3 性能测试

1. 低配机型（≤3GB内存）真机运行30分钟，检查帧率/内存/温度
2. 弱网模拟（微信开发者工具 → 模拟弱网），测试多人模式延迟处理
3. 连续闯关50关，检查内存泄漏

---

## 阶段十一：美术资源替换（第57-63天）

> 此阶段将所有占位资源替换为正式美术资源。

### 步骤 11.1 资源替换流程

1. 美术按规范交付资源（PNG ≤100KB/张，命名统一）
2. 将正式资源放入对应 `textures/` 子目录，替换占位图
3. 更新预制体中的 Sprite 引用 → 指向新资源
4. 逐场景检查显示效果

### 步骤 11.2 动画替换

1. 替换角色待机动画（帧动画/骨骼动画）
2. 替换跳跃/落地/掉落/技能动画
3. 添加方块踩击反馈动画
4. 添加BOSS技能释放动画

### 步骤 11.3 UI替换

1. 替换所有按钮/图标/面板为正式UI资源
2. 调整布局适配（不同机型下UI位置/大小）
3. 替换字体为圆润可爱字体

### 步骤 11.4 音效替换

1. 替换所有BGM为正式制作音频（5个世界+结算+主界面）
2. 替换所有音效为正式音效
3. 调整音量平衡

---

## 阶段十二：上线准备（第64-70天）

### 步骤 12.1 最终构建

1. Cocos Creator 构建微信小游戏版本（Release模式）
2. 检查分包大小：首包≤4MB
3. 在微信开发者工具中上传代码

### 步骤 12.2 提审材料准备

1. 游戏截图（5张，展示核心玩法）
2. 游戏简介（含核心玩法描述）
3. 类目选择（休闲游戏）
4. 隐私协议、用户协议
5. 防沉迷自查报告

### 步骤 12.3 提交审核

1. 在微信公众平台提交审核
2. 审核周期约 1-3 天
3. 审核通过 → 设置发布时间 → 正式上线

### 步骤 12.4 上线监控

1. 实时监控：DAU、崩溃率、加载成功率
2. 第1天重点关注：服务器负载、支付链路、分享链路
3. 准备热更方案：遇到紧急bug可快速发布修复版本

---

## 附录：关键技术选型决策

| 决策点 | 选择 | 原因 |
|--------|------|------|
| 跳跃实现 | Tween + 贝塞尔曲线 | 比物理引擎刚体更可控，手感调参更方便 |
| 碰撞检测 | 手动AABB包围盒 | 方块整齐排列场景下，手动检测比物理引擎更轻量高效 |
| 多人同步 | 微信帧同步 + 本地预测 | 2人场景帧同步足够，实现简单，延迟可接受 |
| 数据存储 | wx.setStorage + 云数据库 | 本地存即时数据（手感好），云端存关键数据（防丢失/排行） |
| 方块生成 | 前方预生成+后方回收 | 滚动窗口模式，内存恒定，不随关卡增长 |
| 状态管理 | 有限状态机（FSM） | 角色/BOSS/关卡状态清晰，方便调试和扩展 |
